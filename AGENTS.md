# 发布约定

每次完成本项目的开发实现任务后，除非用户明确要求不发布或仅做本地修改，均按以下顺序完成交付：

1. 运行与改动相关的本地测试；至少运行 `pytest`，并在适用时运行 `python -m compileall 核心系统 插件 软件界面 设置`。
2. 检查 `git status` 与 `.gitignore`，只暂存本次实现涉及的公开代码、文档和配置。绝不提交 `测试/`、`审计结果/`、`运行记录/`、`模型保存/`、用户导入数据、运行缓存、筛选/导出结果、数据库、日志、`.env` 或凭据。
3. 在 `main` 的新功能分支上提交，并推送至 `origin`。
4. 为该分支创建一个以 `main` 为目标分支的 Draft Pull Request，说明改动内容、原因与实际运行的检查。

若测试失败，不得将该改动作为已验证版本发布；应先报告失败原因或修复后再执行发布流程。
